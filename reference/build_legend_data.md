# Build legend data from present plot elements

Build legend data from present plot elements

## Usage

``` r
build_legend_data(elements, colours, labels)
```

## Arguments

- elements:

  Character vector of element names present in the plot.

- colours:

  Named character vector of colours.

- labels:

  Named character vector of labels.

## Value

Data frame with columns: element, colour, label.
