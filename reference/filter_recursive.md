# Recursively filter site file elements

Recursively filter site file elements

## Usage

``` r
filter_recursive(x, site_filter)
```

## Arguments

- x:

  Site file element (can be data.frame, list, or other)

- site_filter:

  data frame to filter by

## Value

Filtered element
