# Create example LSM intervention data

Creates a simple LSM (Larval Source Management) intervention list for
testing with basic implementation parameters.

## Usage

``` r
create_example_lsm()
```

## Value

A list containing an \`implementation\` data frame with columns: name,
year, day_of_year, lsm_cov
